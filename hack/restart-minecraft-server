#!/usr/bin/env bash
set -euo pipefail

cd "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/.."

if ! command -v pixi >/dev/null 2>&1; then
  echo "missing required command: pixi" >&2
  exit 127
fi

pixi run minerec server stop
exec pixi run minerec server start --wait "$@"
